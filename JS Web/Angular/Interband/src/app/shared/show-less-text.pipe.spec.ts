import { ShowLessTextPipe } from './show-less-text.pipe';

describe('ShowLessTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowLessTextPipe();
    expect(pipe).toBeTruthy();
  });
});
